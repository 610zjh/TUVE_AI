---
retention: permanent
retention_reason: TUVE 全托管模式下需要重点调整的 Skill 口径草案入口。
---

# TUVE Skill Draft

本目录不是把全部 Skill 重新复制一遍，而是先沉一版和 `全托管模式` 最相关的 Skill 草案。

## 当前先写的重点 Skill

- `see2ai_video_generation`

后续可以继续补：

- `see2ai_text_to_image`
- `see2ai_image_to_image`
- `see2ai_text_to_image_uniworld`

## 统一口径

- 在 `先审后发模式` 下，维持原有用户确认门
- 在 `全托管模式` 下，系统默认自己判断并继续执行
- 不再为了形式额外向用户索取一个“确认”
- 交付时要把已做动作和自检项说清楚
